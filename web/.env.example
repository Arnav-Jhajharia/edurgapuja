API_ORIGIN=http://127.0.0.1:8000
SITE_DOMAIN=edurgapuja.app
NEXT_PUBLIC_SITE_DOMAIN=localhost:3000
# "subdomain" (needs a wildcard certificate) or "path" (/p/<slug>, needs none).
NEXT_PUBLIC_PANDAL_ROUTING=subdomain
