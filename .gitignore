__pycache__/
*.py[cod]
.venv/
.env
*.sqlite3
staticfiles/
media/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.DS_Store
node_modules/

# Generated / local-only
.next/
.next-build/
graphify-out/
*.mov
.env.local
.env*.local
# Backups the env-editing scripts leave behind. Real credentials, and they
# were neither ignored nor .env-suffixed, so they were one `git add -A` or one
# `railway up` away from leaving the machine.
.env.bak*
*.env.bak*
.next-preview/
